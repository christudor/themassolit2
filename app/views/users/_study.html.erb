<%= form_for(current_user.relationships.build(course_id: @course.id)) do |f| %>
  <div><%= f.hidden_field :course_id %></div>
  <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>