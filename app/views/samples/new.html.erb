<% provide(:title, 'Add New Sample') %>

<div class="container">
<h1>Add Sample (Stage 2 of 2)</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for @sample, :html => {:multipart => true} do |f| %>
    	<%= render 'shared/sample_error_messages' %>

      <%= f.hidden_field :key %>

      <p>Sample File: <%= @sample.sample_name %></p> 

      <%= f.label :course_id, "Module:" %>
      <%= f.collection_select :course_id, Course.all, :id, :name, {}, { :multiple => false } %>

      <%= f.label :name, "Name:" %>
      <%= f.text_field :name %>  

      <br><br>
      <center>
        <%= f.submit "Add Sample", class: "btn btn-large btn-primary" %>
      </center>
    <% end %>
  </div>
</div>
</div>
