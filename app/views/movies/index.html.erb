<%= content_for :body_id, "datatables" %>
<% provide(:title, 'Lectures Dashboard') %>

<div class="menubar">
    <div class="sidebar-toggler visible-xs">
        <i class="ion-navicon"></i>
    </div>
    
  <div class="page-title">
    Lectures Dashboard
  </div>
</div>

<div class="content-wrapper">
  
  <table id="orders-datatable">
        <thead>
            <tr>
                <th tabindex="0" rowspan="1" colspan="1">Name</th>
                <th tabindex="0" rowspan="1" colspan="1">Lecture</th>
                <th tabindex="0" rowspan="1" colspan="1">Course</th>
                <th tabindex="0" rowspan="1" colspan="1">Tutor</th>
                <th tabindex="0" rowspan="1" colspan="1">Date Added</th>
                <th tabindex="0" rowspan="1" colspan="1">Options</th>
            </tr>
        </thead>
        
        <tfoot>
            <tr>
                <th rowspan="1" colspan="1">Name</th>
                <th rowspan="1" colspan="1">Lecture</th>
                <th rowspan="1" colspan="1">Course</th>
                <th rowspan="1" colspan="1">Tutor</th> 
                <th rowspan="1" colspan="1">Date Added</th>
                <th rowspan="1" colspan="1">Options</th>
            </tr>
        </tfoot>
        <tbody>
            <% Movie.all.each do |movie| %>
            <tr>
              <td><%= movie.name %></td>
              <td><%= link_to movie.lesson.title, movie.lesson %></td>
              <td><%= link_to movie.lesson.course.name, movie.lesson.course %></td>
              <td><%= link_to movie.lesson.course.provider.name, movie.lesson.course.provider %></td>
              <td><%= movie.created_at.to_date.to_formatted_s(:rfc822) %>
              <td>
                  <%= link_to "Del", movie_path(movie), :data => { :confirm => "Delete video?" }, :method => :delete %>
                </td>
            </tr>
            <% end %>
        </tbody>
    </table>
</div>