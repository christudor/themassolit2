<div class="modal fade" id="role-options-<%= subscriber.id %>" tabindex="-1" role="dialog">
      <div class="modal-dialog">
        <div class="modal-content">
          <%= simple_form_for subscriber, :url => subscriber_path(subscriber), :html => {:method => :put } do |f| %>
              <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title" id="myModalLabel">
                  Change role for <strong><%= subscriber.name %></strong> (Current role: 
                    <%= Profile.find_by_id(subscriber.role_ids.first).name.titleize %>)
                </h4>
              </div>
              <div class="modal-body">
                <div class="form-group">
                  <%= f.input :role_ids, :collection => Profile.all, :as => :radio_buttons, :label_method => lambda {|t| t.name.titleize}, :label => false, :item_wrapper_class => 'inline', checked: subscriber.role_ids.first %>
                </div>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                <%= f.submit "Change Role", :class => "btn btn-success" %>
              </div>
        <% end %>
        </div>
      </div>
  </div>