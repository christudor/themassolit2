<%= form_for(current_user.relationships.find_by_course_id(@course),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Quit this course!", class: "btn btn-large" %>
<% end %>