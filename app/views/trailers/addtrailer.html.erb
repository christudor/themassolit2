<% provide(:title, 'Add Trailer') %>
<div class="container">
	<h1>Add Trailer (Stage 1 of 2)</h1>
		<div class="span8 offset2">
		<p class="body">Note: Trailers are added in two stages. On this page, the trailer itself uploaded to Amazon S3. On the next page (which you will be redirected to automatically) that trailer is given a name and linked to a module.</p>
		<br>

		<center><h3>Trailer Uploader</h3>
		<br>
		<%= direct_upload_form_for @traileruploader do |f| %>
        	<p><%= f.file_field :trailer %></p>
        	<p><%= f.submit "Upload trailer", :class => "btn btn-large btn-primary" %></p>
        <% end %>
        </center>
     </div>
</div>